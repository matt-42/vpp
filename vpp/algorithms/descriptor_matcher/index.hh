#pragma once


namespace vpp
{

  // An index must provide:

  //   index_init(subindex, options);
  //   index.index_descriptors(descriptors);
  //   index.search(descriptor) -> match{int match_idx, int distance};

  index1.index_descriptors

  spatial_index_init(options, bruteforce_index());
}
